/**
 * Exercise 01: Sum of multiples
 *
 * Write a TypeScript function sumOfMultiples that calculates the sum of all multiples
 * of a given number up to a specified maximum value.
 * The function should be typesafe, and it should specify the return type
 *
 * Example:
 * sumOfMultiples(3, 10) => 3 + 6 + 9 = 18
 * sumOfMultiples(4, 3) => 0 (no multiples because multiple is greater than 3)
 */

export function sumOfMultiples(multiple, max) {}
